# MCPize Configuration
# Docs: https://mcpize.com/docs/mcpize-yaml

version: 1

name: "Bayut"
slug: "bayut"

runtime: python
entry: "server.py"

build:
  install: "pip install ."

start:
  type: stdio
  command: "python"
  args:
    - "server.py"

bridge:
  mode: stdio
